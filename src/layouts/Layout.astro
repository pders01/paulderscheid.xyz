---
import Nav from "../components/Nav.astro";
import Footer from "../components/Footer.astro";
import "../styles/global.css";

interface Props {
    title: string;
}

const { title } = Astro.props;

const home = "~/paulderscheid.xyz";
const navLinks = [
    { href: "/about", text: "/about" },
    { href: "/blog", text: "/blog" },
    { href: "/links", text: "/links" },
    { href: "mailto:me@paulderscheid.xyz", text: "/mail" },
];
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <link rel="sitemap" href="/sitemap-index.xml" />
        <link rel="alternate" type="application/rss+xml" title="paul derscheid" href="/rss.xml" />
        <meta name="description" content="Paul Derscheid — Software Engineer, Koha contributor." />
        <title>{title}</title>
    </head>
    <body>
        <Nav home={home} links={navLinks} />
        <main>
            <slot />
        </main>
        <Footer />
    </body>
</html>
